<h1><%= @band.name %></h1>
<br>
<% @band.albums.each do |album| %>
  <a href="<%= album_url(album) %>">
    <%= album.name %>
  </a>
  <br>
<% end %>
<br>
<a href=<%= new_band_album_url(@band) %>>Create New Album</a>